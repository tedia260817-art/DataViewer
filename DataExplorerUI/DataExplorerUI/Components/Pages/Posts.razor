@page "/posts"
@inject NavigationManager Navigation
@inject PostService PostService

@using DataExplorerModels;

<h3>Posts</h3>

<NavLink class="btn btn-primary mb-3" href="/">
    <span class="bi bi-house-door"></span> Home
</NavLink>

<button @onclick="TestClick">Click Me</button>

@code {
    private void TestClick()
    {
        Console.WriteLine("Button clicked!");
    }
}

@if (posts == null)
{
    <p>Loading posts...</p>
}
else
{
    @foreach (var post in posts)
    {
        <div class="card mb-2">
            <div class="card-header" @onclick="@(() => Console.WriteLine($"Clicked: {post.Title}"))" style="cursor: pointer;">
                <h5>@post.Title</h5>
            </div>
            @if (post.IsExpanded)
            {
                <div class="card-body">
                    <p>@post.Body</p>
                </div>
            }
        </div>
    }
}

@code {
    private List<Post> posts = new();

    protected override async Task OnInitializedAsync()
    {
        posts = await PostService.GetPostsAsync() ?? new List<Post>();
    }

    private void TogglePost(Post post)
    {
        Console.WriteLine($"Toggled Post ID: {post.Id}, New State: {post.IsExpanded}");
        post.IsExpanded = !post.IsExpanded;
        StateHasChanged();
    }
}
